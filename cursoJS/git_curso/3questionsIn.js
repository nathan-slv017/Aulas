/**
 * what is the scope ?
 * In javaScript ther  are two types of scope:
 * °Local scope
 * °Global scope
 * 
 * Javascript has function scope Each funcion creates a new scope.
 * scope determines the accessibility(visibility) of these variables
 * variables defines inside a function are not acessible (visible) from outside the funciton
 * 
 * 
 * diferença entre 'var' 'let' 'const'
 * 
 * uma variavel do tipo 'var' aceita ser redeclarada varias vezes
 * as variaveis são lidas primeira, assim como as funções
 * 
 * o let possui block scope... so funciona dentro do block de escopo evitando bugs
 * o let so pode ser modificando no proprio valor
 * 
 * const assim como o let, e tbm o valor não pode ser reatribuido
 * 
 * 
 */