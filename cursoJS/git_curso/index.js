/*
   $ cd Desktop/ ..para procurar projetos na pasta desktop
   $ mkdir pasta ... mkdir para criar a pasta e em seguida o nome da pasta
   $ git init ... para iniciar o git
   $ git status ... para ver o status do git
   $ cd pasta ... para entrar na pasta 
   $ git add pasta ... o git add para adicionar e o nome da pasta para adicionar a pasta sitada
   $ git rm 'arquivo' ... para remover o arquivo online
   $ git rm -f 'arquivo' ... para remover completamente o arquivo do computador (não funcional para pastas)
   $ git commit ... para dar commit ou
   $ git commit -m '' ... para dar commit com uma mensagem
 */